
// (function (exports, require, module, __filename, __dirname) {

console.log('-greet/index.js-')

let en = require('./en')
let es = require('./es')

function greet(lang) {
    if (lang === "en") {
        en(); return
    }
    if (lang === "es") {
        es(); return
    }
    if (lang === "ka") {
        console.log('Namaskara'); return
    }
    console.log("give me your lang, to greet you..");
}
// greet();
module.exports = {
    greet
}

// });